import events from "../images/events.png"

const Events = () => {
    return (
        <div className={"flex justify-center align-middle"}>
            <img src={events} alt={"Report"}/>
        </div>
    )
}

export default Events;